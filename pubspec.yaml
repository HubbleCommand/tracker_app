name: tracker_app
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  #flutter_map_tappable_polyline: ^6.0.0 #Removed as doesn't work
  geolocator: ^10.1.0
  path_provider: ^2.1.2
  latlong2: ^0.9.0
  url_launcher: ^6.2.3
  intl: ^0.19.0
  fl_chart: ^0.66.2
  google_polyline_algorithm: ^3.1.0
  strava_client: ^2.1.0 #I can't be asked to do this myself...

  connectivity_plus: ^5.0.2
  battery_plus: ^5.0.2

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
